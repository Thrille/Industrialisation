language: php
php:
  - '7.4.0'

branches:
  only:
    - master
    - Integration

script:
  - echo Integration termine
  - npm install && npm run publish
  
deploy:
  provider: script
  on:
    branch: Integration
  script:
    - ssh thrille@ssh-thrille.alwaysdata.net 
    - cd www/Industrialisation
    - git pull